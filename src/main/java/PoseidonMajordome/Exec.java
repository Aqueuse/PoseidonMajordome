package PoseidonMajordome;

import javax.swing.JFileChooser;

public class Exec {
    // test locally : gradle build + run + console output
    public void testLocally() {
    }

    // create application bundle : gradle fatbuild + JFileChooser to save
    public void bundleApp() {
    }
}
